{
  "scripts": {
    "start": "yarn task --task dev --template cra/default-ts --start-from=install-repo",
    "task": "cd scripts; yarn install >/dev/null; yarn task"
  },
  "packageManager": "yarn@3.2.3"
}

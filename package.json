{
  "scripts": {
    "ci-tests": "cd code; yarn ci-tests",
    "get-report-message": "cd scripts; pnpm run get-report-message",
    "get-template": "cd scripts; pnpm run get-template",
    "lint": "cd code; yarn lint",
    "nx": "cd code; yarn nx",
    "pretty-docs": "cd scripts; pnpm install >/dev/null; pnpm run docs:prettier:write",
    "start": "yarn task --task dev --template react-vite/default-ts --start-from=install",
    "task": "echo 'Installing Script Dependencies...'; cd scripts; pnpm install >/dev/null; pnpm run task",
    "test": "cd code; yarn test"
  },
  "packageManager": "yarn@3.3.0"
}

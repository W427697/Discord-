export const MIN_NAV_WIDTH = 200; // visually there's an additional 10px due to the canvas' left margin
export const MIN_CANVAS_WIDTH = 200;
export const MIN_CANVAS_HEIGHT = 200; // visually it's 50px less due to the canvas toolbar and top margin
export const MIN_PANEL_WIDTH = 220;
export const MIN_PANEL_HEIGHT = 200; // visually it's 50px less due to the panel toolbar
export const DEFAULT_NAV_WIDTH = 230;
export const DEFAULT_PANEL_WIDTH = 400;
export const BREAKPOINT = 600;

import globalThis from 'global';
import { Meta, Story, Canvas } from '@storybook/addon-docs';

<Meta title="vue3/vue3-mdx" />

# Vue3-specific MDX Stories

export const Button = globalThis.Components.Button;

export const Template = (args, { argTypes }) => ({
  props: Object.keys(argTypes),
  components: { MyButton: Button },
  template: '<my-button :color="color" :rounded="rounded" :children="children"></my-button>',
});

<Canvas>
  <Story name="Rounded">
    {{
      components: { MyButton: Button },
      template: '<my-button :rounded="true" :children="A Button with rounded edges" />',
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="Square">
    {{
      components: { MyButton: Button },
      template: '<my-button :rounded="false">A Button with square edges</my-button>',
    }}
  </Story>
</Canvas>

<Canvas>
  <Story name="From Template" args={{ children: 'From template' }}>
    {Template.bind({})}
  </Story>
</Canvas>

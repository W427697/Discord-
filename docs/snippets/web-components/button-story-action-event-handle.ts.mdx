```ts
// Button.stories.ts

import type { Meta } from '@junk-temporary-prototypes/web-components';

import { withActions } from '@junk-temporary-prototypes/addon-actions/decorator';

const meta: Meta = {
  title: 'Button',
  component: 'demo-button',
  parameters: {
    actions: {
      handles: ['mouseover', 'click .btn'],
    },
  },
  decorators: [withActions],
};

export default meta;
```

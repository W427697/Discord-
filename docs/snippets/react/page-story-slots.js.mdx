```js
// Page.stories.js|jsx

import { Page } from './Page';

export default {
  component: Page,
};

export const CustomFooter = {
  render: (args) => (
    <Page {...args}>
      <footer>{args.footer}</footer>
    </Page>
  ),
  args: {
    footer: 'Built with Storybook',
  },
};
```

```js
// Button.stories.js

import { action } from '@storybook/addon-actions';

import Button from './Button.marko';

export default {
  component: Button,
};

export const Text = {
  args: {
    label: 'Hello',
    onClick: action('clicked'),
  },
  render: (input) => ({
    component: Button,
    input,
  }),
};
```

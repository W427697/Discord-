```js
// CSF 2

import Button from './Button.marko';

export default {
  title: 'Button',
  component: Button,
};

export const Primary = (input) => ({
  component: Button,
  input,
});
Primary.args = { primary: true };
```

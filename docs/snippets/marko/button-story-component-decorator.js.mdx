```js
// Button.stories.js

import Button from './Button.marko';
import MarginDecorator from './margin-decorator.marko';

export default {
  component: Button,
  decorators: [
    (Story) => ({
      component: MarginDecorator,
      input: {
        renderBody: Story,
      },
    }),
  ],
};
```

```marko
// margin-decorator.marko

<div style={ margin: '3em' }>
  <${input.renderBody}>
</div>
```

```ts
// Button.stories.ts

export const Primary = …
Primary.decorators = [(storyFn, context) => {
    const story = storyFn(context);
    return {
      ...story,
      template: `<div style={{ margin: '3em' }}>story.template</div>`
    }
  }
]
```

```ts
// Button.stories.ts

import type { Meta } from '@junk-temporary-prototypes/angular';

import { Button } from './button.component';

const meta: Meta<Button> = {
  component: Button,
  parameters: {
    docs: {
      controls: { exclude: ['style'] },
    },
  },
};

export default meta;
```

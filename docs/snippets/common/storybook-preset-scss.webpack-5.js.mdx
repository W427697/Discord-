```shell
# With npm
npm install @junk-temporary-prototypes/preset-scss css-loader sass sass-loader style-loader --save-dev

# With yarn
yarn add --dev @junk-temporary-prototypes/preset-scss css-loader sass sass-loader style-loader

# With pnpm
pnpm add --save-dev @junk-temporary-prototypes/preset-scss css-loader sass sass-loader style-loader
```

```ts
// .storybook/main.ts

// For Vite support add the following import
// import type { AddonOptionsVite } from '@junk-temporary-prototypes/addon-coverage';

import type { AddonOptionsBabel } from '@junk-temporary-prototypes/addon-coverage';

// Replace your-framework with the framework and builder you are using (e.g., react-webpack5, vue3-webpack5)
import type { StorybookConfig } from '@junk-temporary-prototypes/your-framework';

const coverageConfig: AddonOptionsBabel = {
  include: ['**/stories/**'],
  exclude: ['**/exampleDirectory/**'],
  excludeNodeModules: true,
};

const config: StorybookConfig = {
  stories: [],
  addons: [
    // Other Storybook addons
    {
      name: '@junk-temporary-prototypes/addon-coverage',
      options: {
        istanbul: {
          ...coverageConfig,
        },
      },
    },
  ],
};

export default config;
```

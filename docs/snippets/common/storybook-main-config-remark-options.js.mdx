```js
// .storybook/main.js

import remarkGfm from 'remark-gfm';

export default {
  stories: ['../src/**/*.stories.@(js|ts)'],
  addons: [
    // Other addons go here
    {
      name: '@junk-temporary-prototypes/addon-docs',
      options: {
        mdxPluginOptions: {
          mdxCompileOptions: {
            remarkPlugins: [remarkGfm],
          },
        },
      },
    },
  ],
  framework: {
    // Replace your-framework with the name of your framework (e.g., react-webpack5, vue3-webpack5)
    name: '@junk-temporary-prototypes/your-framework',
    options: {},
  },
  docs: {
    autodocs: 'tag',
  },
};
```

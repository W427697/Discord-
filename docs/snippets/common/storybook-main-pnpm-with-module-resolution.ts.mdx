```ts
// .storybook/main.ts

// Replace your-framework with the framework you are using (e.g., react-webpack5, vue3-webpack5)
import type { StorybookConfig } from '@junk-temporary-prototypes/your-framework';

import path from 'path';

const wrapForPnp = (packageName: string) =>
  path.dirname(require.resolve(path.join(packageName, 'package.json')));

const config: StorybookConfig = {
  stories: ['../src/**/*.mdx', '../src/**/*.stories.@(js|jsx|ts|tsx)'],
  addons: [
    //👇 Use wrapForPnp when referencing Storybook's addons and frameworks
    wrapForPnp('@junk-temporary-prototypes/addon-essentials'),
  ],
  framework: {
    // Replace your-framework with the same one you've imported above.
    name: wrapForPnp('@junk-temporary-prototypes/your-framework'),
    options: {},
  },
};

export default config;
```

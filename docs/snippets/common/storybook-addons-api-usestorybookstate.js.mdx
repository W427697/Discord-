```js
// my-addon/src/manager.js|ts

import React from 'react';

import { AddonPanel } from '@junk-temporary-prototypes/components';

import { useStorybookState } from '@junk-temporary-prototypes/manager-api';

export const Panel = () => {
  const state = useStorybookState();
  return (
    <AddonPanel {...props}>
      {state.viewMode !== 'docs' ? (
        <h2>Do something with the documentation</h2>
      ) : (
        <h2>Show the panel when viewing the story</h2>
      )}
    </AddonPanel>
  );
};
```

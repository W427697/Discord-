```js
// .storybook/main.js

export default {
  addons: [
    {
      name: '@junk-temporary-prototypes/preset-scss',
      options: {
        cssLoaderOptions: {
          modules: { localIdentName: '[name]__[local]--[hash:base64:5]' },
        },
      },
    },
  ],
};
```

```js
// storybook.test.js

import path from 'path';

import initStoryshots from '@junk-temporary-prototypes/addon-storyshots';

// The required import from the @junk-temporary-prototypes/addon-storyshots-puppeteer addon
import { imageSnapshot } from '@junk-temporary-prototypes/addon-storyshots-puppeteer';

// Function to customize the snapshot location
const getMatchOptions = ({ context: { fileName } }) => {
  // Generates a custom path based on the file name and the custom directory.
  const snapshotPath = path.join(path.dirname(fileName), 'your-custom-directory');
  return { customSnapshotsDir: snapshotPath };
};

initStoryshots({
  // your own configuration
  test: imageSnapshot({
    // invoke the function above here
    getMatchOptions,
  }),
});
```

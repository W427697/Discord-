```js
// setupFile.js

import { setGlobalConfig } from '@junk-temporary-prototypes/testing-vue';

// Storybook's preview file location
import * as globalStorybookConfig from './.storybook/preview';

setGlobalConfig(globalStorybookConfig);
```
